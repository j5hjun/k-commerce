export const DEFAULT_MCP_SERVERS_JSON = `{
  "mcpServers": {
    "k-commerce": {
      "command": "uv",
      "args": ["run", "k-commerce-mcp"]
    }
  }
}`;

// Grayed-out format hint shown in the empty MCP register textarea.
export const MCP_PLACEHOLDER = DEFAULT_MCP_SERVERS_JSON;
