[project]
name = "k-commerce"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
  "packages/core",
  "packages/mcp",
]

[dependency-groups]
dev = [
  "k-commerce-core",
  "pytest>=8.0",
]

[tool.ruff]
line-length = 200

[tool.pytest.ini_options]
testpaths = ["tests"]