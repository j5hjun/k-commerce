[project]
name = "k-commerce"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
  "packages/cli",
  "packages/mcp",
]

[tool.ruff]
line-length = 200

[tool.pytest.ini_options]
testpaths = ["tests"]
