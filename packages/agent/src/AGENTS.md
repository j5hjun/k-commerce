# Agent Source Root Map

This directory is the Python source root for the agent package.

- `k_commerce_agent/` - agent backend package: FastAPI app, MCP client, and agent build. See `k_commerce_agent/AGENTS.md`.
