# CLI Source Root Map

This directory is the Python source root for the CLI package.

- `k_commerce_cli/` - CLI package modules, commands, services, providers, and terminal adapter. See `packages/cli/src/k_commerce_cli/AGENTS.md`.
