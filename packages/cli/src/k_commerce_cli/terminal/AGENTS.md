# Terminal File Map

This directory contains terminal output adapters.

- `asyncclick.py` - `AsyncClickTerminal`, the concrete terminal adapter used by async click/Typer command handlers.
- `__init__.py` - terminal package exports.

The abstract terminal contract lives in `../base.py`.
