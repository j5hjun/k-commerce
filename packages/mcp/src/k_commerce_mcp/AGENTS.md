# MCP Module File Map

This directory contains the importable `k_commerce_mcp` module.

- `__init__.py` - package marker.
- `server.py` - console script entry point for `k-commerce-mcp`; currently prints a placeholder message until the MCP server is implemented.
