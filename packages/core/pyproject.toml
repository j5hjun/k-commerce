[project]
name = "k-commerce-core"
version = "0.1.0"
description = "Core logic for Korean commerce workflows."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
