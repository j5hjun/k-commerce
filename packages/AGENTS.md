# Packages File Map

This directory contains the workspace packages.

- `cli/` - Typer/asyncclick-based CLI, Coupang provider implementation, browser automation, persistent provider store, and CLI tests. See `packages/cli/AGENTS.md`.
- `mcp/` - MCP server package exposing provider actions as MCP tools. See `packages/mcp/AGENTS.md`.
